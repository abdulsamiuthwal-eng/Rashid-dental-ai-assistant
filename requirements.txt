# =============================================================================
# Rashid Dental AI Assistant — Python Requirements
# =============================================================================
# Project:    Rashid Dental AI Assistant (DEVFORGE Internship — Project 2)
# Phase:      Foundation (Day 1) — all packages pinned for reproducibility
# Python:     3.11+
# Updated:    2026-07-13
#
# INSTALLATION:
#   pip install -r requirements.txt
#
# NOTES:
#   - Use a virtual environment: python -m venv venv
#   - Activate: source venv/bin/activate  (Linux/macOS)
#               venv\Scripts\activate     (Windows)
# =============================================================================


# -----------------------------------------------------------------------------
# Web Framework — FastAPI + ASGI Server
# -----------------------------------------------------------------------------

# FastAPI: high-performance async Python web framework
fastapi==0.111.0

# Uvicorn: ASGI server for running FastAPI (with standard extras)
uvicorn[standard]==0.30.1

# Starlette: underlying ASGI toolkit (pinned via FastAPI, listed for clarity)
starlette==0.37.2

# Python-Multipart: required for form data / file uploads in FastAPI
python-multipart==0.0.9


# -----------------------------------------------------------------------------
# Data Validation & Settings
# -----------------------------------------------------------------------------

# Pydantic: data validation and settings management (FastAPI's foundation)
pydantic==2.7.4

# Pydantic-Settings: .env file loading and settings classes
pydantic-settings==2.3.4

# Email-validator: validates email addresses in Pydantic models
email-validator==2.2.0


# -----------------------------------------------------------------------------
# Environment & Configuration
# -----------------------------------------------------------------------------

# Python-dotenv: loads .env files into environment variables
python-dotenv==1.0.1


# -----------------------------------------------------------------------------
# Google Gemini AI (LLM Provider)
# -----------------------------------------------------------------------------

# Google Generative AI: official Google Gemini Python SDK
google-generativeai==0.7.2


# -----------------------------------------------------------------------------
# LangChain — RAG Pipeline Orchestration
# -----------------------------------------------------------------------------

# LangChain Core: base abstractions (chains, retrievers, memory)
langchain==0.2.6

# LangChain Community: community integrations (FAISS, document loaders)
langchain-community==0.2.6

# LangChain Google GenAI: Gemini integration for LangChain
langchain-google-genai==1.0.7

# LangChain Text Splitters: heading-aware and recursive text chunking
langchain-text-splitters==0.2.2


# -----------------------------------------------------------------------------
# Vector Database — FAISS
# -----------------------------------------------------------------------------

# FAISS CPU: Facebook AI Similarity Search (CPU-only, no GPU required)
faiss-cpu==1.8.0


# -----------------------------------------------------------------------------
# Document Processing — Knowledge Base Ingestion
# -----------------------------------------------------------------------------

# Markdown: converts Markdown to HTML/text for processing
Markdown==3.6

# Python-Markdown-Extensions: enhanced Markdown parsing (tables, fenced code)
pymdown-extensions==10.8.1

# Tiktoken: OpenAI's BPE tokenizer (used for accurate token counting)
tiktoken==0.7.0

# BeautifulSoup4: HTML parsing (useful when processing rendered Markdown)
beautifulsoup4==4.12.3

# lxml: fast XML/HTML parser backend for BeautifulSoup
lxml==5.2.2


# -----------------------------------------------------------------------------
# Database — PostgreSQL + SQLAlchemy ORM
# -----------------------------------------------------------------------------

# SQLAlchemy: SQL toolkit and ORM with async support
sqlalchemy==2.0.31

# AsyncPG: high-performance async PostgreSQL driver (for asyncpg dialect)
asyncpg==0.29.0

# Psycopg2-Binary: standard sync PostgreSQL driver (for migrations/tools)
psycopg2-binary==2.9.9

# Alembic: database schema migrations for SQLAlchemy
alembic==1.13.2


# -----------------------------------------------------------------------------
# Async I/O & File Handling
# -----------------------------------------------------------------------------

# AIOFiles: async file I/O for reading knowledge base documents
aiofiles==23.2.1

# HTTPX: async HTTP client (used for external API calls, testing)
httpx==0.27.0


# -----------------------------------------------------------------------------
# Caching
# -----------------------------------------------------------------------------

# Redis (optional for production caching — comment out if not using)
# redis==5.0.7
# aioredis==2.0.1


# -----------------------------------------------------------------------------
# Security & Authentication
# -----------------------------------------------------------------------------

# Python-Jose: JWT token creation and verification
python-jose[cryptography]==3.3.0

# Passlib: password hashing (bcrypt backend)
passlib[bcrypt]==1.7.4

# Cryptography: required by python-jose
cryptography==42.0.8

# Slowapi: rate limiting middleware for FastAPI (built on limits)
slowapi==0.1.9


# -----------------------------------------------------------------------------
# Logging & Monitoring
# -----------------------------------------------------------------------------

# Loguru: structured, human-friendly logging
loguru==0.7.2

# Rich: rich terminal output for development (colored logs, tables)
rich==13.7.1


# -----------------------------------------------------------------------------
# Testing Framework
# -----------------------------------------------------------------------------

# Pytest: testing framework
pytest==8.2.2

# Pytest-AsyncIO: async test support
pytest-asyncio==0.23.7

# Pytest-Cov: code coverage reporting
pytest-cov==5.0.0

# HTTPX: async test client for FastAPI (already listed above)
# httpx==0.27.0


# -----------------------------------------------------------------------------
# Code Quality & Linting
# -----------------------------------------------------------------------------

# Ruff: extremely fast Python linter and formatter (replaces flake8 + black)
ruff==0.5.0

# MyPy: static type checking
mypy==1.10.1

# Pre-commit: git hooks for automated quality checks
pre-commit==3.7.1


# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------

# Tenacity: retry logic with exponential backoff (for API calls)
tenacity==8.4.2

# Python-slugify: generates URL-friendly slugs
python-slugify==8.0.4

# Arrow: human-friendly datetime handling
arrow==1.3.0

# UUID: built-in Python module — no install needed
